@model WebApplication1.Models.Marca

@{
    ViewBag.Title = "NovaMarca";
}

<h2>NovaMarca</h2>

@using (Html.BeginForm("SalvarMarca", "Marca", FormMethod.Post))
{
    <div class="form-horizontal">
        <h4>Marca</h4>
        <hr />


        @Html.Partial("_FrmMarca")

        <br />

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Salvar" class="btn btn-dark" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("<-- Menu", "Menu", "Usuario")
</div>